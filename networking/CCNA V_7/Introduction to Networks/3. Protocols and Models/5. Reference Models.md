

## The Benefits of Using a Layered Model:
+ A layered model is used to modularize (نمطي) the operations of a network into manageable layers.
+ Assisting (المساعدة) in protocol design because protocols that operate at a specific layer have defined information that they act upon and a defined interface to the layers above and below.
+ Fostering competition because products from different vendors can work together.
+ Preventing technology or capability changes in one layer from affecting other layers above and below.
+ ==Providing a common language to describe networking functions and capabilities.==
+ two layered models that are used to describe network operations:
	+ Open System Interconnection (الترابط) (OSI) Reference Model.
	+ TCP/IP Reference Model.
![[Pasted image 20230212203148.png]]


## The OSI Reference Model:
+ The OSI reference model provides an extensive list of functions and services that can occur at each layer.
+ This type of model provides consistency (تناسق) within all types of network protocols and services by describing what must be done at a particular layer.

| **OSI Model Layer**  | **Description**                                                                                                                                                                                                     |
| :------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **7 - Application**  | The application layer contains protocols used for process-to-process communications.                                                                                                                                |
| **6 - Presentation** | The presentation layer provides for common representation of the data transferred between application layer services.                                                                                               |
| **5 - Session**      | The session layer provides services to the presentation layer to organize its dialogue (حوار) and to manage data exchange.                                                                                          |
| **4 - Transport**    | The transport layer defines services to ==segment, transfer, and reassemble the data for individual communications== between the end devices.                                                                       |
| **3 - Network**      | The network layer provides services to exchange the individual pieces of data over the network between identified end devices.                                                                                      |
| **2 - Data Link**    | The data link layer protocols ==describe methods for exchanging data frames== between devices over a common media                                                                                                   |
| **1 - Physical**     | The physical layer protocols describe the mechanical, electrical, functional, and procedural means to activate, maintain, and de-activate physical connections for a bit transmission to and from a network device. |


## The TCP/IP Protocol Model:
+ TCP/IP is also used as a reference model.
+ The TCP/IP model is a protocol model because it describes the functions that occur at each layer of protocols within the TCP/IP suite.

| **TCP/IP Model Layer** | **Description**                                                             |
| :--------------------- | :-------------------------------------------------------------------------- |
| **4 - Application**    | ==Represents data to the user==, plus encoding and dialog control.          |
| **3 - Transport**      | ==Supports communication between various devices== across diverse networks. |
| **2 - Internet**       | ==Determines the best path through the network.==                           |
| **1 - Network Access** | Controls the ==hardware devices and media== that make up the network.       |


![[Pasted image 20230212203148.png]]


----
# Layers and it's protocols 


## **🟣 Layer 7 — Application Layer**
**Purpose:** 
+ Provides network services directly to user applications (the “what” users interact with).  
**Data Unit:** Data / Messages  
**Examples of Protocols & Concepts:**
- `HTTP`, `HTTPS`
- `FTP`, `TFTP`
- `SMTP`, `POP3`, `IMAP`
- `DNS`, `DHCP`
- `SNMP`, `NTP`
- `SSH`, `Telnet`
- `SIP`, `RTP`, `RTSP`
- `MQTT`, `CoAP`
- `LDAP`, `WHOIS`, `XMPP`
- `SMB`, `NFS`, `RDP`

---

## **🔵 Layer 6 — Presentation Layer**
**Purpose:** 
+ Translates, encrypts, or compresses data so both systems can understand each other.  
**Data Unit:** Data  
**Examples of Protocols & ==Concepts==:**
- `SSL`, `TLS`
- `XDR`, `MIME`, `ASN.1`
- `XML`, `JSON`
- `ASCII`, `UTF-8`, `EBCDIC`
- `JPEG`, `GIF`, `PNG`
- `MPEG`, `MP3`


---

## **🟢 Layer 5 — Session Layer**
**Purpose:** 
+ Manages and controls the dialog (session) between applications — open, maintain, and close sessions.  
**Data Unit:** Data  
**Examples of Protocols & Concepts:**
- `NetBIOS`
- `RPC` (Remote Procedure Call)
- `PPTP`, `L2TP`
- `SAP` (Session Announcement Protocol)
- `SOCKS`
- `ASP` (AppleTalk Session Protocol)


---

## **🟡 Layer 4 — Transport Layer**
**Purpose:** 
+ Ensures reliable (or unreliable) delivery of data, handles segmentation, flow control, and error recovery.  
**Data Unit:** Segments (TCP) / Datagrams (UDP)  
**Examples of Protocols & Concepts:**
- `TCP` (Transmission Control Protocol)
- `UDP` (User Datagram Protocol)
- `SCTP` (Stream Control Transmission Protocol)
- `DCCP` (Datagram Congestion Control Protocol)
- `QUIC` (used in HTTP/3)
- `RUDP` (Reliable UDP)

---

## **🟠 Layer 3 — Network Layer**
**Purpose:** 
+ Determines how data is sent between devices using logical addressing and routing.  
**Data Unit:** Packets  
**Examples of Protocols & Concepts:**
- `IP` (IPv4, IPv6)
- `ICMP` (Internet Control Message Protocol)
- `IGMP` (Internet Group Management Protocol)
- `ARP`, `RARP`
- `IPsec` (Encryption at Layer 3)
- `BGP`, `OSPF`, `RIP`, `IS-IS`, `EIGRP`
- `GRE` (Generic Routing Encapsulation)
- `MPLS` (Layer 2.5)
- `NDP` (Neighbor Discovery Protocol)

---

## **🟤 Layer 2 — Data Link Layer**

**Purpose:** Defines how data is framed, addressed (MAC), and transmitted between devices on the same network.  
**Data Unit:** Frames  
**Examples of Protocols & Concepts:**
- Ethernet (IEEE 802.3)
- `PPP` (Point-to-Point Protocol)
- `HDLC`, Frame Relay
- `ATM`, `FDDI`, Token Ring
- `ARP`, `VLAN` (`802.1Q`)
- `LACP`, `CDP`, `LLDP`
- `Wi-Fi` (IEEE 802.11)
- `Bluetooth` (802.15)
- `MPLS` (when used at Layer 2)

---

## **⚫ Layer 1 — Physical Layer**

**Purpose:** 
+ Transmits raw bits as electrical, optical, or radio signals over the physical medium.  
**Data Unit:** Bits  
**Examples of Protocols & Concepts:**
- Physical media: `UTP`/`STP` cables, Coaxial, Fiber optics
- Interfaces: `RS-232`, `DSL`, `ISDN`, `SONET`, `USB`
- Wireless: Wi-Fi radio, Bluetooth radio, Infrared
- Devices: Hubs, Repeaters, Modems


# Table

| **#** | **Layer Name**         | **Main Function**                                                                              | **Data Unit**                             | **Examples of Protocols / Concepts**                                                                                                                          |
| :---: | :--------------------- | :--------------------------------------------------------------------------------------------- | :---------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **7** | **Application Layer**  | Provides services directly to user applications for network communication.                     | **Data / Messages**                       | HTTP, HTTPS, FTP, TFTP, SMTP, POP3, IMAP, DNS, DHCP, SNMP, NTP, SSH, Telnet, SIP, RTP, RTSP, MQTT, CoAP, LDAP, WHOIS, XMPP, SMB, NFS, RDP                     |
| **6** | **Presentation Layer** | Translates, encrypts, and compresses data for the application layer.                           | **Data**                                  | SSL, TLS, XDR, MIME, ASN.1, XML, JSON, ASCII, JPEG, GIF, PNG, MPEG, MP3, UTF-8, EBCDIC                                                                        |
| **5** | **Session Layer**      | Establishes, manages, and terminates sessions between applications.                            | **Data**                                  | NetBIOS, RPC (Remote Procedure Call), PPTP, L2TP, SAP (Session Announcement Protocol), SOCKS, ASP (AppleTalk Session Protocol)                                |
| **4** | **Transport Layer**    | Provides reliable or unreliable delivery, flow control, segmentation, and error recovery.      | **Segments (TCP)** or **Datagrams (UDP)** | TCP, UDP, SCTP, DCCP, QUIC, RUDP (Reliable UDP)                                                                                                               |
| **3** | **Network Layer**      | Handles logical addressing, routing, and path selection.                                       | **Packets**                               | IP (IPv4, IPv6), ICMP, IGMP, ARP, RARP, IPsec, BGP, OSPF, RIP, IS-IS, GRE, MPLS, EIGRP, NDP                                                                   |
| **2** | **Data Link Layer**    | Defines how data is framed, addressed (MAC), and transmitted between nodes on the same medium. | **Frames**                                | Ethernet (IEEE 802.3), PPP, HDLC, Frame Relay, ATM, FDDI, Token Ring, ARP, VLAN (802.1Q), LACP, CDP, LLDP, Wi-Fi (802.11), Bluetooth (802.15), MPLS (Layer 2) |
| **1** | **Physical Layer**     | Converts bits to signals and defines electrical, optical, and radio interfaces.                | **Bits**                                  | UTP/STP cables, Fiber optics, Coaxial, RS-232, DSL, ISDN, SONET, USB, Bluetooth radio, Wi-Fi radio, Infrared, Hubs, Repeaters                                 |

- **ARP** (Address Resolution Protocol) can be seen as between **Layer 2 and Layer 3**, since it maps IP addresses (Layer 3) to MAC addresses (Layer 2).

- **SSL/TLS** can sometimes appear in both **Layer 6 (Presentation)** and **Layer 7 (Application)**, depending on the model used.

- **QUIC** (used in HTTP/3) is a modern **Transport Layer** protocol replacing TCP in some cases.

- **MPLS** (Multi-Protocol Label Switching) sits between Layer 2 and Layer 3 — sometimes called _Layer 2.5._