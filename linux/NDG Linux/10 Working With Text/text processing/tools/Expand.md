# Replace tabs by spaces


## Expand  and un-expand command:
+ The `expand` command replace the tab characters within the ﬁle with the corresponding number of spaces.
+ The `unexpand` program to substitute tabs for spaces.
```shell
>> expand file_name
```

