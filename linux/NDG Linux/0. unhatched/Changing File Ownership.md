

Initially, the owner of a file is the user who creates it.


## The `chown` command:
```
chown [OPTIONS] [OWNER] FILE
```
+ To change the user owner of a file, the following syntax can be used. The first argument, `[OWNER]`, specifies which user is to be the new owner.
+ The second argument, `FILE`, specifies which file's ownership is changing.