
# String concatenation
It can be useful to combine two strings together. 
This process is called _string concatenation_, and we can use the concatenation operator (`.`) to do this.

```php
echo "one" . "two"; // Prints: onetwo
```
