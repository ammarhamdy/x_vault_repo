
# Install JMeter Plugins Manager

If you don’t already have the **Plugins Manager**:
1. Download `PluginsManager.jar` from https://jmeter-plugins.org/get/
2. Place it in the `lib/ext/` directory of your JMeter installation.
3. Restart JMeter.
4. Go to **Options > Plugins Manager** in the JMeter menu.

